/** Single-page owner UI shell. Filled in by the view implementation. */
export function renderPage(): string {
  return `<!doctype html>
<html lang="en">
<head><meta charset="utf-8"><title>aperture</title></head>
<body>aperture ui</body>
</html>`;
}
